import 'package:flutter/material.dart';

class MNDivider extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: 1,
      color: Theme.of(context).secondaryHeaderColor,
    );
  }
}